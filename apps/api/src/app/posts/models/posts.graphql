type Post {
  id: String!
  test: String
  title: String!
  description: String!
  created: String!
  updated: String!
  comments: [Comment!]
  author: User
  upvotes: Int
  downvotes: Int
}

type Query {
  posts(page: Int): [Post]
}
